#python3 
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:

        x = {}
        for i,j in enumerate (nums):
            r = target - j
            if r in x : return [x[r],i]
            x[j]=i
    
